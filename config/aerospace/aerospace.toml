# AeroSpace config — migrated from yabai/skhd
# See: https://nikitabobko.github.io/AeroSpace/guide

config-version = 2

start-at-login = true

# Normalizations (default, keeps layout tidy)
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

accordion-padding = 30

# Tiles = BSP equivalent from yabai
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'

# Mouse follows focus when switching monitors
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

automatically-unhide-macos-hidden-apps = false

# Notify sketchybar on workspace change
exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE']

# 7 persistent workspaces (matching old yabai labels)
persistent-workspaces = ['1', '2', '3', '4', '5', '6', '7']

[key-mapping]
    preset = 'qwerty'

# Gaps — similar to yabai (4px inner, 4px outer, 20px top for sketchybar)
[gaps]
    inner.horizontal = 4
    inner.vertical =   4
    outer.left =       4
    outer.bottom =     4
    outer.top =        20
    outer.right =      4

# === Main keybindings ===
[mode.main.binding]

    # Terminal
    cmd-shift-semicolon = 'workspace 2'
    cmd-shift-enter = 'exec-and-forget kitty @ --to "unix:$(ls -t /tmp/kitty-* | head -1)" launch --type=os-window --cwd=~'

    # Layout
    alt-slash = 'layout tiles horizontal vertical'
    alt-comma = 'layout accordion horizontal vertical'

    # Focus (hjkl)
    alt-h = 'focus left'
    alt-j = 'focus down'
    alt-k = 'focus up'
    alt-l = 'focus right'

    # Move windows
    alt-shift-h = 'move left'
    alt-shift-j = 'move down'
    alt-shift-k = 'move up'
    alt-shift-l = 'move right'

    # Resize
    alt-minus = 'resize smart -50'
    alt-equal = 'resize smart +50'

    # Workspaces
    alt-1 = 'workspace 1'
    alt-2 = 'workspace 2'
    alt-3 = 'workspace 3'
    alt-4 = 'workspace 4'
    alt-5 = 'workspace 5'
    alt-6 = 'workspace 6'
    alt-7 = 'workspace 7'

    # Move window to workspace
    alt-shift-1 = 'move-node-to-workspace 1'
    alt-shift-2 = 'move-node-to-workspace 2'
    alt-shift-3 = 'move-node-to-workspace 3'
    alt-shift-4 = 'move-node-to-workspace 4'
    alt-shift-5 = 'move-node-to-workspace 5'
    alt-shift-6 = 'move-node-to-workspace 6'
    alt-shift-7 = 'move-node-to-workspace 7'

    # Toggle back to previous workspace
    alt-tab = 'workspace-back-and-forth'

    # Move workspace to another monitor
    alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

    # Fullscreen and float
    alt-f = 'fullscreen'
    alt-shift-space = 'layout floating tiling'

    # Enter service/apps modes
    alt-shift-semicolon = 'mode service'
    alt-a = 'mode apps'

# === Service mode (for less common operations) ===
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main']    # reset layout
    f = ['layout floating tiling', 'mode main']    # toggle float
    backspace = ['close-all-windows-but-current', 'mode main']

    alt-shift-h = ['join-with left', 'mode main']
    alt-shift-j = ['join-with down', 'mode main']
    alt-shift-k = ['join-with up', 'mode main']
    alt-shift-l = ['join-with right', 'mode main']

# === Apps mode (quick launcher) ===
[mode.apps.binding]
    esc = 'mode main'
    a = ['exec-and-forget open /Applications/Arc.app', 'mode main']
    b = ['exec-and-forget open /Applications/zoom.us.app', 'mode main']
    c = ['exec-and-forget open /System/Applications/Calendar.app', 'mode main']
    e = ['exec-and-forget open /Applications/Emacs.app', 'mode main']
    f = ['exec-and-forget open /Applications/Firefox.app', 'mode main']
    g = ['exec-and-forget open /Applications/Google\ Chrome.app', 'mode main']
    m = ['exec-and-forget open /Applications/Microsoft\ Outlook.app', 'mode main']
    p = ['exec-and-forget open /Applications/Spotify.app', 'mode main']
    s = ['exec-and-forget open /Applications/Slack.app', 'mode main']
    v = ['exec-and-forget open /Applications/Visual\ Studio\ Code.app', 'mode main']
    z = ['exec-and-forget open /Applications/Zen.app', 'mode main']
    space = ['exec-and-forget open /Applications/kitty.app', 'mode main']

# === Window rules ===
# Workspace assignments (1=emacs, 2=terminal, 3=mail, 4=web, 5=calendar, 6=social, 7=media)

[[on-window-detected]]
if.app-name-regex-substring = '^Emacs$'
run = 'move-node-to-workspace 1'

[[on-window-detected]]
if.app-name-regex-substring = '^kitty$'
if.during-aerospace-startup = true
check-further-callbacks = true
run = 'move-node-to-workspace 2'

[[on-window-detected]]
if.app-name-regex-substring = '^Mail'
run = 'move-node-to-workspace 3'

[[on-window-detected]]
if.app-name-regex-substring = '^Microsoft Outlook$'
run = 'move-node-to-workspace 3'

[[on-window-detected]]
if.app-name-regex-substring = '^Safari$'
run = 'move-node-to-workspace 4'

[[on-window-detected]]
if.app-name-regex-substring = '^Firefox$'
run = 'move-node-to-workspace 4'

[[on-window-detected]]
if.app-name-regex-substring = '^Arc$'
run = 'move-node-to-workspace 4'

[[on-window-detected]]
if.app-name-regex-substring = '^Google Chrome$'
run = 'move-node-to-workspace 4'

[[on-window-detected]]
if.app-name-regex-substring = '^Zen$'
run = 'move-node-to-workspace 4'

[[on-window-detected]]
if.app-name-regex-substring = '^Microsoft Outlook$'
if.window-title-regex-substring = 'Calendar'
run = 'move-node-to-workspace 5'

[[on-window-detected]]
if.app-name-regex-substring = '^Slack$'
run = 'move-node-to-workspace 6'

[[on-window-detected]]
if.app-name-regex-substring = '^Spotify$'
run = 'move-node-to-workspace 7'

# Floating windows (manage=off equivalent)

[[on-window-detected]]
if.app-name-regex-substring = '^Finder$'
if.window-title-regex-substring = '(Co(py|nnect)|Move|Info|Pref)'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^macfeh$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^System (Preferences|Settings)$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^App Store$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^Activity Monitor$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^mpv$'
run = 'layout floating'

[[on-window-detected]]
if.window-title-regex-substring = 'Software Update'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^GlobalProtect$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^Telegram$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^Microsoft Teams$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^zoom.us$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^BlueJeans$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^Messages$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'WhatsApp'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'Cisco Webex'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^Inkscape$'
run = 'layout floating'
